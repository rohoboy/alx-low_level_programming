#include "main.h"
/**
 *_islower - Entry point for this function
 *@c: the character to be checked
 * Return: 1 for lower character or 0 for other
 */

int _islower(int c)
{
	if (c >= 97)
	{
		return (1);
	}
	return (0);
}
