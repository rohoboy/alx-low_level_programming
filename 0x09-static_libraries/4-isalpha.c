#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _isalpha -function to check for alphabet
 * @c: input
 * Return: alphabet
 */
int _isalpha(int c)
{
	return (isalpha(c));
}

